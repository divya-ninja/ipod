import React, {Component} from 'react';
import './SideComponent.css';

class Settings extends Component{
    render(){
        return(
            <div id="settings" className="side-component">
                <h3 id="settings-text">Settings</h3>
            </div>
        );
    }
}

export default Settings;